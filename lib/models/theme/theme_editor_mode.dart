/// Represents different modes for the theme editor.
///
/// This enumeration defines different modes that can be used in the theme editor. It includes [simple] mode and [whatsapp] mode.
enum ThemeEditorMode {
  /// Represents a simple mode for the theme editor.
  simple,

  /// Represents a WhatsApp-themed mode for the theme editor.
  whatsapp,
}
