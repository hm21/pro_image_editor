// Flutter imports:
import 'package:flutter/widgets.dart';

/// Primary color used in the image editor interface.
const imageEditorPrimaryColor = Color(0xFF004C9E);

/// Color for the app bar in the image editor.
const imageEditorAppBarColor = Color(0xFF000000);

/// Background color for the image editor.
const imageEditorBackgroundColor = Color(0xFF161616);

/// Text color used in the image editor interface.
const imageEditorTextColor = Color(0xFFE1E1E1);
