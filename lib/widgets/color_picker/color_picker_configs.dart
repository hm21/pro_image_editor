/// An enumeration representing different modes for color picking.
enum PickMode {
  /// Color mode: Allows picking from a range of colors.
  color,

  /// Grey mode: Allows picking from shades of grey.
  grey,
}
