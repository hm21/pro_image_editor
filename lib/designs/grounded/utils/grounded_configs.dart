// ignore_for_file: constant_identifier_names

/// The duration used for fade-in animations in the Grounded design.
const GROUNDED_FADE_IN_DURATION = Duration(milliseconds: 220);

/// The stagger delay between multiple fade-in animations in the Grounded
/// design.
const GROUNDED_FADE_IN_STAGGER_DELAY = Duration(milliseconds: 25);

/// The height of the sub-bar used in the Grounded design.
const GROUNDED_SUB_BAR_HEIGHT = 65.0;
